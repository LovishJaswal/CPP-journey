#include<iostream>
using namespace std;
int main() {
    char arr[15] = {"HelloWorld"}; // Declare and initialize a character array
    for(int i = 0; i < 10; i++) { // Loop through the array
        cout << arr[i]; // Print each character
    }
    return 0;
}